{
  "name": "geto-subagents",
  "version": "0.2.1",
  "description": "Fork-based tmux-hosted subagents for pi: /review, /find, /plan, /chain, and a generic subagent tool. Each subagent is a detached pi process whose session FORKS the main conversation (full context inherited, child session visible in /resume), reports its result back, and is attachable via pi --attach-subagent <id>.",
  "type": "module",
  "keywords": [
    "pi-package",
    "geto",
    "subagents",
    "tmux",
    "fork",
    "chain",
    "review",
    "planner",
    "finder"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mr-jones123/geto.git"
  },
  "files": [
    "index.ts",
    "agents.ts",
    "README.md"
  ],
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-tui": "^0.84.1",
    "typebox": "^1.3.11"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
