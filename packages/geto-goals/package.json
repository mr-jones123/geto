{
  "name": "geto-goals",
  "version": "0.2.1",
  "description": "Durable autonomous goal loop for pi (Codex /goal style). /goal set|show|edit|pause|resume|clear plus a goal_report tool and an agent_settled continuation loop. Goal state lives in .pi/goals/goal.json and is re-injected every turn, so it survives compaction, /new, and restarts.",
  "type": "module",
  "keywords": [
    "pi-package",
    "geto",
    "goals",
    "autonomous",
    "loop",
    "agent"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mr-jones123/geto.git"
  },
  "files": [
    "index.ts",
    "README.md"
  ],
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "typebox": "^1.3.11"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
